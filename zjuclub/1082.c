#include <stdio.h>

int main(void)
{
    int n;
    while (scanf("%d", &n) == 1)
    {
        char ch[10];
        int temp = 0x01;
        while ((temp & n) != 0)
        {

        }
    }

    return 0;
}
