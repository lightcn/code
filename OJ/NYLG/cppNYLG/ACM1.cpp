#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n;
    char a[4];
    while (cin>>n)
    {
    	for (int i=0; i<n; ++i)
    	    char a[i]=0;
	    for (int j=0; j<n; ++j)
        {
        	vector<char> a[j](4);
        	cin.getline(a[j], 4);
       		a[j].push_back();
        	for ()
        }
    }
    return 0;
} 