#include <stdio.h>
#include <stdint.h>

int main(void)
{
    int n;
    printf("MAXINT=%d, MININT=%d\n", INT32_MAX, INT32_MIN);
//    while (scanf("%d", &n) == 1) 
//    {
//        for (int i = 0; i < n; i++)
  //          ;
    //}
    return 0;
}
